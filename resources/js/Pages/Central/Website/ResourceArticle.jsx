import { Head } from '@inertiajs/react';
import WebsiteLayout from './components/WebsiteLayout';
import { WebsiteContainer, WebsiteImage, WebsiteLink } from './components/WebsitePrimitives';
export default function ResourceArticle({ article, ...layout }) { return <WebsiteLayout {...layout}><Head title={article.seo_title || article.title}><meta name="description" content={article.meta_description || article.excerpt || ''}/></Head><WebsiteContainer as="article" className="kl-resource-article"><WebsiteLink href="/resources">← All resources</WebsiteLink><p>{article.category?.name}</p><h1>{article.title}</h1><p className="kl-lede">{article.excerpt}</p>{article.featured_media&&<WebsiteImage src={article.featured_media.url} alt={article.featured_media.alt_text || article.title} fit="cover" loading="eager"/>}<div className="kl-prose" dangerouslySetInnerHTML={{__html:article.body}}/>{article.gallery?.length>0&&<div className="kl-resource-gallery">{article.gallery.map(image=><WebsiteImage key={image.id} src={image.url} alt={image.alt_text || article.title} fit="cover"/>)}</div>}</WebsiteContainer></WebsiteLayout>; }
