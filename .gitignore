*.log
.DS_Store
Thumbs.db

 

.phpactor.json
.phpunit.result.cache
.phpunit.cache/

.codex/
.claude/
.cursor/
.idea/
.nova/
.vscode/
.zed/

auth.json

# Anchored to repo root with a leading slash — an unanchored "vendor/" also
# matches resources/views/vendor/ (the published installer views) and would
# silently drop them from the package, breaking /install with "view not found".
/node_modules/
/vendor/



_ide_helper.php

Homestead.json
Homestead.yaml

# Environment — never commit real credentials/keys (ship .env.example only)
.env
.env.backup
.env.production
.env.*.local

# Local dev database — must not ship in the repo or package
 

# Generated at runtime by the installer / Laravel
/storage/app/installed
/storage/app/install/
 
/dist/
